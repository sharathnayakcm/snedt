
-if !request.xhr? || @new_record == true
  .columns
    .width_15
      .privacy_group_panel
        - rounded_box_for( |
            nil, |
            :header => {:text => "Privacy Groups", :level => 4, :color => "green"}, |

            :group_name => "privacy_group" |
          ) do |panel| |
          #privacy_group_list= render :partial => "privacy_group_list"
      #form_content
        = render :partial => "new"
-else
  #privacy_group_list= render :partial => "privacy_group_list"

