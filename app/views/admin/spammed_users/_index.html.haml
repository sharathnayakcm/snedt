.columns
  .width_23
    -if !request.xhr? || @new_record == true
      .resource_panel
        - rounded_box_for( |
            nil, |
            :header => {:text => "Manage Spammed Users", :level => 4, :color => "green"}, |
            :footer => { :color => "chrome"}, |
            :group_name => "resource" |
          ) do |rounded_box| |
          = render :partial => "spammed_users_list"
    -else
      = render :partial => "spammed_users_list"