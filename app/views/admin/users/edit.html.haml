:javascript
  $j(document).ready(function(){
    round_corner();
    hide_all();
    empty_all();
    sign_up_validations();
  });

.left_content
  - form_for :user, @user, :url => { :controller => "admin/users", :action => "update", :id => @user.id, :admin_id => current_user.id }, :method => "put" do |f|
    = render :partial => "admin/users/form", :locals => { :f => f }

:javascript
  dashBoardMenuHiglightion('manage');