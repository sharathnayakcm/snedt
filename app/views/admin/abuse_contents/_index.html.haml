.columns
  .width_24
    -if !request.xhr? || @new_record == true
      .resource_panel
        - rounded_box_for( |
            nil, |
            :header => {:text => "Manage Abuse Contents", :level => 4, :color => "green"}, |
            :footer => { :color => "chrome"}, |
            :group_name => "abuse_contents" |
          ) do |rounded_box| |
          = render :partial => "abuse_contents_list"
    -else
      = render :partial => "abuse_contents_list"
- total_flag =  (@abuse_contents.total_entries.to_i % (params[:per_page].blank? ? 10 : params[:per_page]).to_i)
//= update_rounded_box_actions( |
//    [ |
//      link_to_remote_redbox("#{t :add_rss}", {:url => new_admin_admin_rss_link_path, :method => :get}, :class => "button") |
//    ], |
//    :group_name => 'admin_rss_links' |
//  ) |
