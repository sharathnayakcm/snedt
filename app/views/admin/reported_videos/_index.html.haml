.columns
  .width_23
    -if !request.xhr? || @new_record == true
      .resource_panel
        - rounded_box_for( |
            nil, |
            :header => {:text => "Manage Reported Videos", :level => 4, :color => "green"}, |
            :footer => { :color => "chrome"}, |
            :group_name => "resource" |
          ) do |rounded_box| |
          = render :partial => "reported_videos_list"
    -else
      = render :partial => "reported_videos_list"