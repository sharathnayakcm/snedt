.columns
  .width_23
    -if !request.xhr? || @new_record == true
      .resource_panel
        - rounded_box_for( |
            nil, |
            :header => {:text => "Priview #{@notification.name}", :level => 4, :color => "green"}, |
            :footer => { :color => "chrome"}, |
            :group_name => "static_pages" |
          ) do |rounded_box| |
          %br
          #to== To: user@email.com
          %br
          #from
            == From:
            == noreply@edintity.com
          %br
          #subject
            ==Subject
            = @notification.subject
          %br
          %hr
          #notifications_content= @notification.content
          %br
          %br
          #links.align_right= link_to "Back to Manage Notifications", admin_notifications_path(current_user)

:javascript
  dashBoardMenuHiglightion('manage');