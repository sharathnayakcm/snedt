.columns
  .width_23
    -if !request.xhr? || @new_record == true
      .resource_panel
        - rounded_box_for( |
            nil, |
            :header => {:text => "Edit topic", :level => 4, :color => "green"}, |
            :footer => { :color => "chrome"}, |
            :group_name => "topics" |
          ) do |rounded_box| |
          .rounded_box_content_padding
            - form_for(@topic, :url => admin_topic_path(current_user, :id => @topic.id), :html => { :multipart => true }, :method => :put) do |f|
              = render :partial => "form", :locals => {:f => f}

:javascript
  dashBoardMenuHiglightion('manage');