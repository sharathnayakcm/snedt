.columns
  .width_23
    -if !request.xhr? || @new_record == true
      .resource_panel
        - rounded_box_for( |
            nil, |
            :header => {:text => "New Service", :level => 4, :color => "green"}, |
            :footer => { :color => "chrome"}, |
            :group_name => "services" |
          ) do |rounded_box| |
          .rounded_box_content_padding
            - form_for(@service, :url => admin_services_path(current_user), :method => :post) do |f|
              = render :partial => "form", :locals => {:f => f}

:javascript
  dashBoardMenuHiglightion('manage');