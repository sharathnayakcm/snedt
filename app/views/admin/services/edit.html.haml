.columns
  .width_23
    -if !request.xhr? || @new_record == true
      .resource_panel
        - rounded_box_for( |
            nil, |
            :header => {:text => "Edit Service", :level => 4, :color => "green"}, |
            :footer => { :color => "chrome"}, |
            :group_name => "services" |
          ) do |rounded_box| |
          .rounded_box_content_padding
            - form_for(@service, :url => admin_service_path(current_user, :id => @service.id), :method => :put) do |f|
              = render :partial => "form", :locals => {:f => f}

:javascript
  dashBoardMenuHiglightion('manage');