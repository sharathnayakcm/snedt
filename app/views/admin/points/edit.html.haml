.columns
  .width_23
    -if !request.xhr? || @new_record == true
      .resource_panel
        - rounded_box_for( |
            nil, |
            :header => {:text => "Edit point", :level => 4, :color => "green"}, |
            :footer => { :color => "chrome"}, |
            :group_name => "points" |
          ) do |rounded_box| |
          .rounded_box_content_padding
            - form_for(@point, :url => admin_point_path(current_user, :id => @point.id), :html => { :multipart => true }, :method => :put) do |f|
              = render :partial => "form", :locals => {:f => f}

:javascript
  dashBoardMenuHiglightion('manage');